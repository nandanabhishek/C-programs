#include<stdio.h>
#include<math.h>
int main()
{
    int n,i,a,rev=0;
    printf("enter the no to reverse it: ");
    scanf("%d",&n);
    while(n>0)
    {
        a=n%10;
        rev=rev*10+a;
        n=n/10;
    }
    printf("reversed no = %d",rev);

}
